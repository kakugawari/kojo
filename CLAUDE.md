# ねこ工場 (kojo)

かわいいねこたちが働く工場を、だんだん大きくしていく放置ゲーム。
素の HTML / CSS / JS だけで動く。ビルドはしない。

---

## 何のアプリか

アイソメトリック (ななめ見おろし) の工場。部屋のまんなかをベルトコンベアが通り、
その両側にねこが立って流れ作業をしている。時間が経つと勝手にお金がたまり、
そのお金で部署のレベルを上げ、にくきゅう🐾でガチャを引いてねこを増やしていく。

- **部署 (ぶしょ)** — ごはん工房🍙 / くみたてライン🔧 / けんぴんライン🔍 /
  けいと工房🧶 / しゅっか場📦 / きゅうけい室🛋️ の 6 つ。
  部署ごとに機械 (釜・プレス機・検査ゲート・糸車・クレーン) と、
  ベルトを流れるもの (おにぎり・歯車・箱・毛糸玉・木箱) がちがう。
  レベルを上げるともうけが増える。きゅうけい室だけはベルトがなく、
  もうけを出すかわりに工場ぜんたいの倍率を上げる
- **工場レベル** — 「建てた部署のレベルの合計 + ねこの数 + 1」。
  **どこにも保存していない。** state から必ず同じ値が出るので、ズレようがない。
  上げると次の部署がひらく
- **ねこ** — レア度 (N / R / SR / UR) と見た目 9 種の組み合わせ。
  持ち場のある部署に配属すると、その部署のもうけを上げる。
  配属先の色の前かけをつける。
  にくきゅう🐾でレベルアップできる
- **さわる** — ねこをタップすると なでられて、1 秒ぶんのお金が入る。
  部署の上にときどき浮かぶ「あわ」をタップすると、30 秒ぶんか にくきゅう
- **時間** — 実時間 1 秒 = ゲーム内 1 分 (1 日 24 分)。
  朝・昼・夕・夜で空の色が変わる。留守のあいだも働く (8 時間ぶんまで、4 割)

## 動かす・試す

```bash
npm start          # http://localhost:8080/ で開く
npm test           # ロジックのテスト (node だけで動く。速い)
npm run test:ui    # ブラウザで実際に動かすテスト (要 npm i -D playwright)
npm run shot       # 画面を撮って目で見る (node shot.js 出力先)
npm run icon       # icon.svg から PNG のアイコンを作り直す
npm run build:single   # 全部を 1 枚の HTML にまとめる
```

`npm run test:ui` は本物のブラウザを立ち上げ、指の操作をそのまま再現する。
**画面を触ったときの不具合はこちらでしか捕まらない。** 変更したら必ず両方流す。

手元の Chromium を使いたいときは `CHROMIUM_PATH=/path/to/chrome npm run test:ui`。

## 構成

```
index.html / styles.css   画面のわく
core.js                   ロジック。DOM を触らない。ブラウザと node の両方で動く
app.js                    工場の絵、指の操作、パネル
core.test.js              ロジックのテスト (node --test)
browser-test.js           ブラウザで動かすテスト
shot.js                   画面を撮る
build-single.js           1 枚の HTML にまとめる
make-icon.js              icon.svg → icon-180.png / icon-512.png
serve.js                  開発用サーバー
```

ファイルはすべて直下に置く。スマホやタブレットからでも
1 ファイルずつ差し替えられるようにするため。

`window.__app` はテストから中身をのぞくための入口。`browser-test.js` が使う。

## この工場の決めごと

- **数はぜんぶ core.js で出す。** もうけ、値段、レベル、時計。
  app.js は受け取って描くだけ
- **保存するのは「事実」だけ。** お金・部署のレベル・ねこ。
  そこから計算できるもの (もうけ/秒、工場レベル) は保存しない
- **セーブは必ず開ける。** `deserialize` は壊れた JSON も足りない項目も
  新品の値で埋めて返す。「セーブが壊れて開けない」を仕組みで防ぐ
- **絵は 1 枚の SVG。** カメラ (`#camera`) の transform だけを動かすので、
  ずらしても拡大しても当たり判定を計算し直さなくていい
- **画面の座標に置くものは、カメラの外の層に置く。**
  看板 (`#layerSigns`)、あわと「+◯◯」(`#layerFx`)。
  カメラの中に入れると座標が二重にかかる
- **絵を描き直すのは、中身が変わったときだけ** (`buildScene`)。
  毎フレームやるのは上のバーの文字だけで、しかも変わったときだけ書き換える
- **持ち場はベルトの両側。** `core.slotPositions` が `side` (-1 = 奥 / +1 = 手前) を返し、
  app.js は「奥のねこ → ベルト → 手前のねこ」の順に重ねる
- **部署の名前を変えたら、前のセーブを読みかえる。** `OLD_ROOM_IDS` に
  古い id → 新しい id を書けば、遊んでいた工場がそのまま続く

---

## 作り方(毎回これをなぞる)

### 1. 土台を決める

- **正しさを仕組みで保証できないか、先に考える。** 「あとで検算する」より
  「そもそも間違いようがない作り」の方が強い
  (例: 工場レベルを保存せず、毎回 state から数える)
- **ロジックを画面から切り離す。** `core.js` は DOM を触らない。
  だから node でテストできる
- **ビルドを挟まない。** 素の HTML / CSS / JS

### 2. 確かめる

- **「テストが通った」で終わらせない。実際に動かして目で見る。**
  `npm run shot` で撮って、1 枚ずつ見る
- **体感の問題は計測する。** CPU を 4 倍遅くして 1 フレームの時間を測る
  (`browser-test.js` の「遅い端末」)
- **端やずらした条件でも試す。** 原点(0,0)や初期状態だけで試すと見逃す
- **直したら見張りのテストを足す。** 同じ失敗を二度しないための唯一の方法

### 3. テストプレイしてもらう

1. `npm run build:single -- --fragment` で 1 枚にまとめる
2. Artifact として公開し、リンクを渡す。スマホでもすぐ開ける
3. **そのリンクは Claude 側の仮ページ。** アドレスもタブのアイコンも Claude のものになる。
   本番のアドレスとは別だと必ず伝える
4. 直したら同じリンクを更新する(新しいリンクを作らない)

### 4. git と公開

- **作業用ブランチで進める。main へは、言われてから出す**
- コミットは「何を、なぜ」。直した不具合は**原因**まで書く。日本語でよい
- **push する前に `npm test` と `npm run test:ui` を両方流す**

### 5. 報告のしかた

- 直したことは事実で書く。**計測値や試した件数を添える**
- できていないこと、飛ばしたことは、そのまま言う

---

## 踏んだ落とし穴(同じ轍を踏まないために)

画面まわりは、動いているように見えて壊れていることがある。実際に踏んだもの:

| 症状 | 原因 | 直し方 |
| --- | --- | --- |
| 画面ぜんたいに灰色の幕がかかり、空の白い箱が出る | `hidden` は CSS の `display` に負ける。`#modalWrap` に `display:grid` を書いていた | `#modalWrap[hidden] { display: none }` も書く |
| タップで開いたパネルが、その場で閉じる | 指のタップのあと、ブラウザは click も送る。開いたばかりのパネルの裏にそれが当たっていた | 開いて 400ms は裏押しを受け付けない |
| あわが画面いっぱいの白い帯になる | 画面の座標で置いたものを、カメラの中の層に入れていた。translate と scale が二重にかかる | 演出の層はカメラの外に出す |
| ズームすると看板の字が読めない/でかすぎる | 看板をカメラの中に置いていたので、一緒に拡大された | 看板も画面の座標に置き、カメラを動かすたびに置き直す |
| 置いた直後に要素が一瞬とぶ | CSS アニメーションの `transform: scale` が、位置決めの `transform: translate` を上書きしていた | 外側の `<g>` に位置、内側の `<g>` にアニメーション |
| アニメーションが 1 フレームも動かない | 同じフレーム内で書き換えると、ブラウザは途中の状態を見ない | `element.animate()` (Web Animations API) を使う |
| ゆれている絵を Playwright がタップできない (`element is not stable`) | ずっと動いているものは「安定」しない | 位置を測って `page.touchscreen.tap(x, y)` でつつく |
| ねこをタップしたのに部署のパネルが開く | ねこの絵はすき間だらけで、真ん中が透明だった | テストではからだ (`.body`) をねらう |
| 合成した PointerEvent で操作が途中で止まる | `setPointerCapture` が、実在しない pointerId で例外を投げていた | try/catch で囲む |
| ホーム画面のアイコンが違うものになる | iOS は `apple-touch-icon` に SVG を使えない | `make-icon.js` で PNG を作る |
| 操作のたびに一瞬止まる | 操作ごとに `localStorage` へ同期で書き込み、全体を描き直していた | 書き込みは 3 秒ごとにまとめる。描き直しは変わったときだけ |
| タップしても反応しない | 上に重ねた層が全面をふさいでいた | `#hud` は `pointer-events: none`、中身だけ `auto` |
| ガチャの結果が 1 秒たらずで消える | 板を 0.6 秒ごとに描き直していて、あとから innerHTML で足した中身が流された | 出すものは変数に持ち、`body()` の中から描く |
| SVG の中の絵を回すと、あらぬ所を軸に回る | CSS の `transform` は、SVG 要素だと絵の真ん中ではなく SVG 全体の原点が軸になる | 回す要素に `transform-box: fill-box` を指定する |

**新しく踏んだら、この表に足すこと。**
