# ねこイライラ棒 (repo: kojo)

ねこの輪っかを、カベに当てずにゴールまで運ぶゲーム。
素の HTML / CSS / JS だけで動く。ビルドはしない。

> リポジトリ名の `kojo` は、前に置いていた工場ゲームの名ごり。
> 中身はイライラ棒。名前を変えるときは GitHub 側で。

---

## 何のアプリか

細いコースを、指で持った棒の先の輪っかで通していく。カベにさわる、
じゃまものにさわる、とちゅうで指をはなす — どれもその場でやり直し。

- **あそびかた** — 画面のどこかを指で押すと、指の 96 うえに輪っかが出る
  (指で輪っかが隠れないように、棒のぶんだけ持ち上げている)。
  START の台に輪っかを入れた瞬間に時計が動きだし、GOAL の台に入れば上がり
- **コース** — 6 ステージ。だんだん細く、じゃまものが増える
- **じゃまもの** — 往復する棒 (slide) / 回る腕 (rotor) / ふくらむ玉 (pulse)。
  動きは毎回まったく同じ (走り出した瞬間から数える) ので、覚えれば必ず通せる
- **記録** — ステージごとの自己ベスト。クリアすると次がひらく

## いちばん大事な作り

**コースは「1 本の折れ線 (path) を太らせたもの」として持つ。**

- カベの判定 = 「折れ線までの距離 > コース半径 − 輪っか半径」、それだけ
- 絵も同じ折れ線を `stroke-width = 半径×2` で描くだけ。
  **見た目と当たり判定が同じ 1 本から出るので、ズレようがない**
  (ブラウザのテストが、描いた太さと判定の太さが同じ数字かを見張っている)
- 中心線をたどれば必ずゴールに着く。行き止まりのあるコースを作れない

**じゃまものは座標を手で書かない。** ステージに書くのは型と向きと周期だけ。
置き場所は `buildStages` が、次の条件を満たす場所をならべて均等に配る:

1. 曲がり角から離れている
   (角のそばに置くと、角の内側と外側を同時にふさいで通れなくなる)
2. スタート台・ゴール台から離れている
3. 盤からはみ出さない
4. **ふさぐ範囲が、ほかのじゃまものとかさならない**
   → どの一点も、狙ってくるのは多くて 1 つ。1 つずつ待てば必ず抜けられる

**そのうえで、全ステージを自動プレイヤー (`ghostRun`) に通させる。**
中心線の上だけを歩き、「どこに・いつ居られるか」を 1 コマずつ塗りつぶす
到達可能性の全探索なので、待つ・下がるを含めた全部の動きを試している。
これが通らないコースは置けない (`npm test` が落ちる)。

## 動かす・試す

```bash
npm start          # http://localhost:8080/ で開く
npm test           # ロジックのテスト (node だけで動く。速い)
npm run test:ui    # ブラウザで実際に動かすテスト (要 npm i -D playwright)
npm run shot       # 画面を撮って目で見る (node shot.js 出力先)
npm run icon       # icon.svg から PNG のアイコンを作り直す
npm run build:single   # 全部を 1 枚の HTML にまとめる
```

`npm run test:ui` は本物のブラウザを立ち上げ、指の操作をそのまま再現する。
**画面を触ったときの不具合はこちらでしか捕まらない。** 変更したら必ず両方流す。
中心線をなぞってステージ 1 を実際に通し切るところまでやっている。

手元の Chromium を使いたいときは `CHROMIUM_PATH=/path/to/chrome npm run test:ui`。

## 構成

```
index.html / styles.css   画面のわく
core.js                   ロジック。DOM を触らない。ブラウザと node の両方で動く
app.js                    コースの絵、指の操作、音
core.test.js              ロジックのテスト (node --test)
browser-test.js           ブラウザで動かすテスト
shot.js                   画面を撮る
build-single.js           1 枚の HTML にまとめる
make-icon.js              icon.svg → icon-180.png / icon-512.png
serve.js                  開発用サーバー
```

ファイルはすべて直下に置く。スマホやタブレットからでも
1 ファイルずつ差し替えられるようにするため。

`window.__app` はテストから中身をのぞくための入口。`browser-test.js` が使う。
`clientForRing(x, y)` は「輪っかをこの盤座標に置きたいとき、指はどこを押せばいいか」を返す。

## このゲームの決めごと

- **数はぜんぶ core.js で出す。** 当たり判定、置き場所、時間の書き方。
  app.js は受け取って描くだけ
- **遊びは盤の座標 (640 × 960) だけで組み立てる。** 画面の大きさへの変換は
  SVG の viewBox にまかせる。どの端末でもコースの細さが同じになる
- **指の動きは細かく分けて、途中もぜんぶ調べる** (`sweepTo`)。
  点だけで見ていると、すばやく払ったときにコマとコマのあいだでカベをまたぐ
- **じゃまものの時計は、走り出した瞬間から数える。** 毎回おなじ動きになるので、
  タイムが運で決まらない
- **セーブは必ず開ける。** `deserialize` は壊れた JSON も足りない項目も
  新品の値で埋めて返す。記録があるステージの次は、必ずひらいた状態に直す
- **台 (START / GOAL) の中は安全地帯。** 絵もその通りに描く。
  そうしないと「台に置いたのにコースからはみ出して即アウト」になる

---

## 作り方(毎回これをなぞる)

### 1. 土台を決める

- **正しさを仕組みで保証できないか、先に考える。** 「あとで検算する」より
  「そもそも間違いようがない作り」の方が強い
  (例: コースを折れ線から作れば、クリアできないコースを置けない)
- **ロジックを画面から切り離す。** `core.js` は DOM を触らない。
  だから node でテストできる
- **ビルドを挟まない。** 素の HTML / CSS / JS

### 2. 確かめる

- **「テストが通った」で終わらせない。実際に動かして目で見る。**
  `npm run shot` で撮って、1 枚ずつ見る
- **体感の問題は計測する。** CPU を 4 倍遅くして 1 フレームの時間を測る
  (`browser-test.js` の「遅い端末」)
- **端やずらした条件でも試す。** 原点(0,0)や初期状態だけで試すと見逃す
- **直したら見張りのテストを足す。** 同じ失敗を二度しないための唯一の方法
- **見張りそのものも見張る。** 「通れないコース」をわざと作って、
  ghostRun がちゃんと落ちることを確かめている

### 3. テストプレイしてもらう

1. `npm run build:single -- --fragment` で 1 枚にまとめる
2. Artifact として公開し、リンクを渡す。スマホでもすぐ開ける
3. **そのリンクは Claude 側の仮ページ。** アドレスもタブのアイコンも Claude のものになる。
   本番のアドレスとは別だと必ず伝える
4. 直したら同じリンクを更新する(新しいリンクを作らない)

### 4. git と公開

- **作業用ブランチで進める。main へは、言われてから出す**
- コミットは「何を、なぜ」。直した不具合は**原因**まで書く。日本語でよい
- **push する前に `npm test` と `npm run test:ui` を両方流す**

### 5. 報告のしかた

- 直したことは事実で書く。**計測値や試した件数を添える**
- できていないこと、飛ばしたことは、そのまま言う

---

## 踏んだ落とし穴(同じ轍を踏まないために)

画面まわりは、動いているように見えて壊れていることがある。実際に踏んだもの:

| 症状 | 原因 | 直し方 |
| --- | --- | --- |
| 指をすばやく払うと、カベをすり抜けてゴールできる | 判定を「いまの点」だけで見ていて、コマとコマのあいだを飛ばしていた | 前の場所から今の場所までを細かく分けて、途中もぜんぶ調べる |
| どうやっても通れないコースができる | じゃまものを曲がり角のそばに置き、角の内側と外側を同時にふさいでいた | 置き場所は角から離れた直線の途中だけにする |
| 待っても抜けられない組み合わせができる | 2 つのじゃまものの「ふさぐ範囲」がかさなっていた | 範囲がかさならない置き方しか選ばない |
| 回る腕がコースの別の場所まで巻きこむ | 折り返したコースでは、腕が並んだ通路の両方に届いてしまう | ふさぐ範囲が広すぎる置き方は使わない (上限を決める) |
| テストに渡した座標が空になる | `SVGPoint` をそのまま返していた。ブラウザの外へは持ち出せない | ふつうの `{x, y}` に直して返す |
| 画面ぜんたいに灰色の幕がかかる | `hidden` は CSS の `display` に負ける | `#modalWrap[hidden] { display: none }` も書く |
| タップで開いたパネルが、その場で閉じる | 指のタップのあと、ブラウザは click も送る | 開いて 400ms は裏押しを受け付けない |
| 置いた直後に要素が一瞬とぶ | CSS アニメーションの `transform: scale` が、位置決めの `transform: translate` を上書きしていた | 外側の `<g>` に位置、内側の `<g>` にアニメーション |
| アニメーションが 1 フレームも動かない | 同じフレーム内で書き換えると、ブラウザは途中の状態を見ない | `element.animate()` (Web Animations API) を使う |
| ゆれている絵を Playwright がタップできない (`element is not stable`) | ずっと動いているものは「安定」しない | 位置を測って `page.touchscreen.tap(x, y)` でつつく |
| 合成した PointerEvent で操作が途中で止まる | `setPointerCapture` が、実在しない pointerId で例外を投げていた | try/catch で囲む |
| ホーム画面のアイコンが違うものになる | iOS は `apple-touch-icon` に SVG を使えない | `make-icon.js` で PNG を作る |
| タップしても反応しない | 上に重ねた層が全面をふさいでいた | `#hud` は `pointer-events: none`、中身だけ `auto` |
| SVG の中の絵を回すと、あらぬ所を軸に回る | CSS の `transform` は、SVG 要素だと絵の真ん中ではなく SVG 全体の原点が軸になる | 回す要素に `transform-box: fill-box` を指定する |
| 広い範囲を置きかえたら、下にあった関数がまるごと消えた | 置換の終わり位置を目で確かめずに切った | 置きかえたら、すぐ `npm test` を流す (このときは 1 分で気づけた) |

**新しく踏んだら、この表に足すこと。**
